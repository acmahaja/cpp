int foo(int x); // Prototype declaration of foo, see by main
                // Must specify return type name, and argument list types
int foo(int x)  // Must watch the prototype
{
    // Body of foo
    std::cout << x << "\n";
}
